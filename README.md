# Fashion-MNIST-CNN-Image-Classifier-PyTorch-
This project builds a Convolutional Neural Network (CNN) using PyTorch to classify images. The model learns visual patterns from grayscale images of clothing items and predicts the category of each image.
